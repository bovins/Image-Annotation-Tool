# 🖍️ 图像标注工具（Image Annotation Tool）

一个纯前端、可直接在浏览器运行的图像标注网页。打开 `index.html` 即可使用（无需安装、无需服务器；
fabric.js 已内置在 `vendor/`，也可离线运行）。

## 功能总览

### 上传图片
- 点击顶栏「上传图片」，或直接把图片拖入画布区域，或 `Ctrl+V` 粘贴剪贴板图片。
- 长边超过 4096px 的图片会自动等比缩小，保证流畅。

### 多图标签页
- 可同时打开多张图片：再上传一张图会**新建一个标签页**，顶栏标签栏可随时切换。
- 每张图片的标注、图层、撤销/重做历史、缩放位置、马赛克**各自独立保存**，切换互不影响。
- 标签右侧「✕」关闭当前图片；关闭所有标签页后可重新上传。
- 点击「＋ 新图」或再次上传图片即可继续添加标签页。

### 无限画布
- 画布为无限大小：可在图片之外任意位置标注，标注与图片各自独立。
- **滚轮缩放**（以鼠标为中心）；**按住空格 + 左键拖动** 或 **按住鼠标中键拖动** 平移画布。
- 顶栏「− / 100% / + / 适应窗口」控制缩放，「适应窗口」自动居中图片。
- 顶栏「🌙 / ☀️」按钮切换**暗色 / 亮色主题**，选择会被记住。

### 标注工具（左侧工具栏，共 5 类 20 个工具）
| 分类 | 工具 |
| --- | --- |
| 基础类 | 文字（按下放置，拖动同时调整大小）、矩形（白色描边）、消息框、直线、**曲线（样条，锚点可拖动）**、手写、**箭头（实心箭头 + 白色描边）**、**曲线箭头（样条，3-4 个可拖动锚点）**、步骤序号 |
| 尺规类 | 尺寸（长度测量）、角度（三点测角）、面积（多边形面积） |
| 引出类 | **文字引出（箭头锚点 + 可自由拖动的文字，引出线自动连接、无方框）**、**引出区域（区域→引出线→水平线→线上文字，无方框，区域/文字均可拖动）**、图引出（框选区域放大成图块）、**多引出（多点汇聚到一条水平线，文字在线上、无方框）** |
| 放大镜 | 圆形放大镜、方形放大镜（实时显示放大内容，可拖动、调倍数） |
| 贴图类 | 表情贴图（48 种内置 + 自定义图片上传）、马赛克（笔刷涂抹，可擦除） |

- 文字：按住拖动可同时调整文字大小（拖动距离越大字越大）；选中后拖角点等比缩放，字号自动合并到面板显示值。
- 曲线 / 曲线箭头：按住拖动绘制；画完后**选中曲线，拖动蓝色圆点锚点即可调整曲线形状**。
- **直线 / 箭头**：画完后选中，**拖动蓝色圆点端点**即可调整位置与方向。
- 文字引出：按下设定箭头锚点，拖动到目标位置放文字；**水平线长度只取决于文本长度**，拖动文字/锚点时**引出箭头线自动伸缩直连**（不再固定直角）；选中后拖动蓝色圆点可分别调整锚点与文字位置，拖动引出线整体移动。
- 引出区域：按住拖动画出椭圆区域；样式为**区域 → 引出箭头线 → 水平线 → 线上文字（文字无方框）**。选中后拖动蓝色圆点：椭圆中心为「区域」手柄，文字中心为「文字」手柄，可分别调整。

### 图层系统（右侧面板底部）
- **每个标注会自动创建属于自己的图层**，并按「标注 1」「标注 2」…自动命名，新图层置于最顶层（新标注默认画在最上层）。
- **锁定**（🔒）：锁定后该图层所有对象不可选中、不可编辑。
- **显隐**（👁 / 🚫）：隐藏后该图层全部对象不显示。
- **拖动排序**：按住行首 ≡ 拖动到目标行，即可调整上下层遮挡关系（列表上方 = 顶层）。
- 删除图层会同时删除该图层上的标注（可撤销）。
- **图层操作全部进入撤销历史**：新建图层、删除图层、拖动排序、显隐、锁定/解锁都可以用 `Ctrl+Z` 撤销 / `Ctrl+Y` 重做。

### 标注初始大小随图片自适应
- 所有标注的初始大小（文字字号、步骤序号半径、贴图大小、引出线偏移、线宽等）会根据图片尺寸自动缩放：小图缩小、大图放大，避免在大图上标注过小。之后仍可在面板中单独调整。

### 样式调整（右侧面板）
- **文字样式**：字体、字号、字距、颜色、粗体/斜体/下划线、轮廓颜色、轮廓粗细。
- **图形样式**：边框色、线宽、线形（实线/虚线/点线/点划线）、圆角、填充透明、填充颜色。
- **引出样式**：引出线颜色/线宽/线形；文字引出可切换引出方向（向左/向右）。
- **测量**：比例尺（px/单位）与单位名称，设置后尺寸/面积按单位显示。

面板在「工具选中时」显示该工具的默认样式；「选中画布对象后」显示该对象样式（可直接修改已画的内容）。

### 常用操作与快捷键
- 每完成一个标注，工具会自动切回「选择 / 移动」并选中刚画的对象，方便立即调整样式或位置（马赛克除外，可连续涂抹）。
- 双击文字对象 → 快速在右侧编辑文字内容。
- `Ctrl+Z` 撤销 / `Ctrl+Y`（或 `Ctrl+Shift+Z`）重做。
- `Delete` 删除选中对象 / `Ctrl+D` 复制选中对象。
- `Ctrl+S` 保存工程 / `Ctrl+E` 导出 PNG。
- **方向键微调**：选中对象后按方向键 1px 微调位置（按住 Shift 为 10px）。
- **多选对齐**：Shift 多选后，右侧面板可一键左对齐 / 水平居中 / 右对齐 / 顶部对齐 / 垂直居中 / 底部对齐。
- `Esc` 取消当前工具 / 多步工具（角度/面积/多引出）中途取消。
- `V` 切换回「选择 / 移动」。
- 面积、多引出：右键或 `Enter` 完成当前步骤。
- 面板中的字号、线宽、圆角等数字输入支持实时生效（输入即应用）。

### 全局样式与记忆（侧边栏底部）
- 侧边栏底部「**全局样式**」区可统一调整**所有标注的默认样式**：线条（颜色/线宽/线形/描边）、文字（字体/字号/字距/颜色/粗斜体/下划线/轮廓）、图形（边框/圆角/填充）、阴影开关。
- 点「🔄 应用到已有标注」可把当前全局样式一次性套用到画布上所有已有标注。
- 选中单个对象时，右侧「外观」区可单独关闭该对象的阴影；「线条/图形样式」区可单独调整描边色与描边粗细。
- **样式记忆**：所有样式设置（线条/文字/图形/阴影/贴图/马赛克/测量比例/放大镜/主题）都会存入浏览器本地，下次打开自动恢复上次的调整。

### 界面动效
- 主题切换平滑过渡、按钮按压反馈、面板轻淡入、Toast 滑入、空状态呼吸提示、新标注放置淡入、缩放/适应窗口平滑过渡、导出时转圈遮罩。
- 动效全部使用 transform/opacity 合成属性（不触发重排，性能开销极小）；若系统开启「减少动态效果」，自动停用装饰性动画。

### 导出与工程
- **导出选项面板**（右侧「画布」面板底部）：可设置**分辨率**（1× 原尺寸 / 0.5× 缩小 / 2× 放大）、**JPG 质量**（50–100%）、以及是否**包含图片外的标注**；设置会自动记住。
- **导出 PNG**：无损、**图片外的区域为透明背景**；自动计算画布边界，**图片 + 全部标注（含图片外的标注）都会包含在导出结果中**。编码在后台异步进行，不卡界面。
- **📋 复制 PNG**：一键把标注结果复制到剪贴板（图片），可直接粘贴到聊天窗口 / 文档 / 画图中（Chrome 109+ 支持；旧浏览器自动降级并提示）。
- **导出 JPG**：体积小、速度快（质量可调，透明区域以白色填充），适合照片类图片。
- **体积说明**：如果原图是 JPEG（照片），PNG 导出会以**无损**方式重新编码整张照片，照片在 PNG 下体积会明显变大（900KB 的 JPEG 照片导出 PNG 约 4~6MB 属正常现象）。需要小体积时请用「导出 JPG」。
- **保存工程**：保存为 JSON（含图片、图层、全部标注与样式），之后可「载入工程」继续编辑。Chrome/Edge 会弹出**「另存为」对话框让你选择存放位置**；Firefox 及以 `file://` 方式打开时自动降级为直接下载。`Ctrl+S` 也可保存。

## 文件结构
```
index.html            页面入口
css/style.css         界面样式
js/objects.js         自定义标注对象（文字/消息框/步骤/引出/测量/放大镜等）
js/tools.js           工具手势交互与马赛克图层
js/app.js             面板、历史记录、导出、工程存取、快捷键
vendor/fabric.min.js  Fabric.js 5.5.2（本地依赖，离线可用）
test-verify.js        对象库验证脚本（node test-verify.js）
test-render.js        渲染与序列化集成测试（node test-render.js）
test-tools.js         工具交互集成测试（node test-tools.js）
test-app-smoke.js     app.js 冒烟测试：多图标签页/历史/导出选项（node test-app-smoke.js）
```

## 浏览器兼容
- 需要支持 Canvas 2D 的现代浏览器（Chrome / Edge / Firefox / Safari 均可）。
- **Windows 7 可用**：请使用 Chrome 109+、Firefox 115 ESR+ 或 Edge 109+（Win7 最后支持的版本）；**不支持 IE11**。打开页面时如遇“浏览器过于老旧”提示，即需更换浏览器。
- 注意：Windows 7 没有内置彩色 emoji 字体，「贴图」类表情会显示为黑白/方框；上传自定义图片贴图不受影响，其余功能全部正常。
